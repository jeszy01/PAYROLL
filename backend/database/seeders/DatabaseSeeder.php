<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * No sample/demo data is seeded here on purpose — this system ships
     * empty and ready to receive real records once it's in front of a
     * client. Add your own seeders here only for local testing, and keep
     * them out of any production deploy.
     */
    public function run(): void
    {
        //
    }
}
