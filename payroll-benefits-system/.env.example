# Copy this file to .env and adjust values for your environment.
# The frontend only needs the API base URL — the centralized backend
# owns the actual PostgreSQL and Redis connections.

# Base URL of the centralized backend API this frontend talks to.
VITE_API_BASE_URL=http://localhost:8000/api
